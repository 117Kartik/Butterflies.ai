import Link from 'next/link'; import { Clock, MoreHorizontal, Users } from 'lucide-react'; import { Meeting } from '@/lib/types';
function duration(n:number){return `${Math.floor(n/60)}h ${n%60}m`;}
export default function MeetingCard({meeting}:{meeting:Meeting}){return <Link href={`/meetings/${meeting.id}`} className="group block rounded-xl border border-slate-200 bg-white p-5 shadow-card transition hover:-translate-y-0.5 hover:border-violet-200 hover:shadow-md"><div className="mb-5 flex justify-between"><div className={`grid h-11 w-11 place-items-center rounded-xl text-lg font-bold ${meeting.color||'bg-violet-100 text-violet-600'}`}>{meeting.title.charAt(0)}</div><button onClick={e=>e.preventDefault()} className="text-slate-400 opacity-0 transition group-hover:opacity-100"><MoreHorizontal size={20}/></button></div><h2 className="truncate text-[15px] font-semibold text-slate-800">{meeting.title}</h2><p className="mt-1 text-xs text-slate-500">{new Date(meeting.date).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'})}</p><div className="my-4 h-px bg-slate-100"/><div className="flex items-center justify-between text-xs text-slate-500"><span className="flex items-center gap-1.5"><Clock size={14}/>{duration(meeting.duration)}</span><span className="flex items-center gap-1.5"><Users size={14}/>{meeting.participants.length}</span><div className="flex -space-x-1.5">{meeting.participants.slice(0,3).map((p,i)=><span key={p} className={`grid h-6 w-6 place-items-center rounded-full border-2 border-white text-[9px] font-bold ${['bg-orange-100 text-orange-600','bg-blue-100 text-blue-600','bg-emerald-100 text-emerald-600'][i]}`}>{p.slice(0,1)}</span>)}</div></div></Link>}
