'use client';
import Link from 'next/link'; import { FormEvent, useState } from 'react'; import { useRouter } from 'next/navigation'; import Shell from '@/components/shell'; import { createMeeting } from '@/lib/api';
export default function NewMeeting(){const router=useRouter(),[title,setTitle]=useState(''),[date,setDate]=useState(new Date().toISOString().slice(0,16)),[duration,setDuration]=useState('30'),[participants,setParticipants]=useState(''),[error,setError]=useState(''),[saving,setSaving]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setSaving(true);setError('');try{const meeting=await createMeeting({title,date:new Date(date).toISOString(),duration:Number(duration),participants:participants.split(',').map(x=>x.trim()).filter(Boolean)});router.push(`/meetings/${meeting.id}`)}catch(err){setError(err instanceof Error?err.message:'Could not save meeting.')}finally{setSaving(false)}};return <Shell><section className="mx-auto max-w-2xl px-8 py-12"><p className="text-sm font-medium text-lilac">Manual entry</p><h1 className="mt-1 text-3xl font-bold dark:text-white">Create a meeting</h1><p className="mt-2 text-slate-500">Add a meeting now; connect your calendar later.</p><form onSubmit={submit} className="mt-8 space-y-4 rounded-xl border bg-white p-6 shadow-card dark:border-slate-800 dark:bg-slate-900"><label className="block text-sm font-medium dark:text-white">Meeting name<input value={title} onChange={e=>setTitle(e.target.value)} required className="mt-2 w-full rounded-lg border p-2.5 text-slate-900" placeholder="e.g. Design review"/></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm font-medium dark:text-white">Date and time<input value={date} onChange={e=>setDate(e.target.value)} required type="datetime-local" className="mt-2 w-full rounded-lg border p-2.5 text-slate-900"/></label><label className="block text-sm font-medium dark:text-white">Duration (minutes)<input value={duration} onChange={e=>setDuration(e.target.value)} min="1" required type="number" className="mt-2 w-full rounded-lg border p-2.5 text-slate-900"/></label></div><label className="block text-sm font-medium dark:text-white">Participants <span className="font-normal text-slate-400">(separate names with commas)</span><input value={participants} onChange={e=>setParticipants(e.target.value)} className="mt-2 w-full rounded-lg border p-2.5 text-slate-900" placeholder="Maya Chen, Alex Morgan"/></label>{error&&<p className="text-sm text-red-600">{error}</p>}<div className="flex gap-3"><button disabled={saving} className="rounded-lg bg-lilac px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{saving?'Saving…':'Save meeting'}</button><Link href="/" className="rounded-lg border px-4 py-2 text-sm dark:text-white">Cancel</Link></div></form></section></Shell>}
