'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { Eye, EyeOff } from 'lucide-react'; import { findWorkspaceUser } from '@/lib/supabase-auth';
export default function Login(){const router=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await findWorkspaceUser(email,password);router.push('/')}catch(err){setError(err instanceof Error?err.message:'Could not continue.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-violet-50 p-5"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-card"><Link href="/" className="text-xl font-bold">Butterflies <span className="text-lilac">ai</span></Link><h1 className="mt-8 text-2xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Enter your email and password to return to your workspace.</p><form onSubmit={submit}><input value={email} onChange={e=>setEmail(e.target.value)} required className="mt-6 w-full rounded-lg border p-3" placeholder="Email address" type="email"/><div className="relative mt-3"><input value={password} onChange={e=>setPassword(e.target.value)} required className="w-full rounded-lg border p-3 pr-11" placeholder="Password" type={show?'text':'password'}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'} className="absolute right-3 top-3 text-slate-500">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="mt-5 w-full rounded-lg bg-lilac p-3 text-sm font-semibold text-white disabled:opacity-60">{busy?'Checking…':'Continue'}</button></form><p className="mt-5 text-center text-sm text-slate-500">New to Butterflies? <Link className="font-semibold text-lilac" href="/signup">Create a profile</Link></p></section></main>}
