'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { signIn } from '@/lib/supabase-auth';
export default function Login(){const router=useRouter(),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');try{await signIn(email,password);router.push('/')}catch(err){setError(err instanceof Error?err.message:'Could not sign in.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-violet-50 p-5"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-card"><Link href="/" className="text-xl font-bold">Butterflies <span className="text-lilac">ai</span></Link><h1 className="mt-8 text-2xl font-bold">Welcome back</h1><p className="mt-2 text-sm text-slate-500">Sign in to your meeting workspace.</p><form onSubmit={submit}><input value={email} onChange={e=>setEmail(e.target.value)} required className="mt-6 w-full rounded-lg border p-3" placeholder="Email address" type="email"/><input value={password} onChange={e=>setPassword(e.target.value)} required className="mt-3 w-full rounded-lg border p-3" placeholder="Password" type="password"/>{error&&<p className="mt-3 text-sm text-red-600">{error}</p>}<button disabled={busy} className="mt-5 w-full rounded-lg bg-lilac p-3 text-sm font-semibold text-white disabled:opacity-60">{busy?'Signing in…':'Sign in'}</button></form><p className="mt-5 text-center text-sm text-slate-500">New to Butterflies? <Link className="font-semibold text-lilac" href="/signup">Create an account</Link></p></section></main>}
