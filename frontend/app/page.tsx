'use client';
import { useEffect, useMemo, useState } from 'react'; import Shell from '@/components/shell'; import MeetingCard from '@/components/meeting-card'; import { getMeetings } from '@/lib/api'; import { Meeting } from '@/lib/types'; import { ChevronDown, Filter, Search, SlidersHorizontal } from 'lucide-react'; import { motion } from 'framer-motion'; import Link from 'next/link';
const demo:Meeting[]=[{id:1,title:'Product Strategy Sync',date:'2026-07-24T09:00:00',duration:48,participants:['Maya Chen','Alex Morgan','Sam Lee'],summary:'',key_topics:[],chapters:[],transcript:[],action_items:[],color:'bg-violet-100 text-violet-600'},{id:2,title:'Q3 Marketing Planning',date:'2026-07-23T09:00:00',duration:36,participants:['Jordan Kim','Avery Patel'],summary:'',key_topics:[],chapters:[],transcript:[],action_items:[],color:'bg-pink-100 text-pink-600'},{id:3,title:'Weekly Design Critique',date:'2026-07-22T09:00:00',duration:57,participants:['Noah Davis','Maya Chen','Liam Scott'],summary:'',key_topics:[],chapters:[],transcript:[],action_items:[],color:'bg-sky-100 text-sky-600'}];
export default function Dashboard(){const [meetings,setMeetings]=useState<Meeting[]>(demo),[search,setSearch]=useState(''),[loading,setLoading]=useState(true),[order,setOrder]=useState<'newest'|'oldest'>('newest'),[filterOpen,setFilterOpen]=useState(false);useEffect(()=>{const id=setTimeout(()=>getMeetings(search).then(setMeetings).catch(()=>{}).finally(()=>setLoading(false)),250);return()=>clearTimeout(id)},[search]);const shown=useMemo(()=>[...meetings].sort((a,b)=>order==='newest'?+new Date(b.date)-+new Date(a.date):+new Date(a.date)-+new Date(b.date)),[meetings,order]);return <Shell><section className="mx-auto max-w-[1440px] px-5 py-8 sm:px-8"><div className="mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-1 text-sm font-medium text-lilac">Your workspace</p><h1 className="text-3xl font-bold tracking-tight dark:text-white">Your meetings</h1><p className="mt-2 text-sm text-slate-500">Review, search and revisit every conversation.</p></div><Link href="/meetings/new" className="rounded-lg border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold shadow-sm dark:border-slate-700 dark:bg-slate-900"><span className="mr-2 text-lilac">✦</span>Join a meeting</Link></div><div className="mb-6 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><div className="relative max-w-md flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search meetings by name" className="w-full rounded-lg border border-slate-200 bg-white py-2.5 pl-10 pr-3 text-sm text-slate-800 outline-none focus:border-lilac focus:ring-2 focus:ring-violet-100 dark:border-slate-700 dark:bg-slate-900 dark:text-white"/></div><div className="flex gap-2"><button onClick={()=>setFilterOpen(!filterOpen)} className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-600 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-300"><Filter size={16}/>All meetings<ChevronDown size={15}/></button><button onClick={()=>setOrder(order==='newest'?'oldest':'newest')} className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-600 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-300"><SlidersHorizontal size={16}/>{order==='newest'?'Newest':'Oldest'}<ChevronDown size={15}/></button></div></div>{filterOpen&&<div className="mb-5 rounded-lg border border-violet-100 bg-violet-50 p-3 text-sm text-violet-800 dark:border-violet-900 dark:bg-violet-950">Showing all meeting types. Additional filters will appear as your workspace grows.</div>}<div className="mb-4 flex items-center justify-between"><h2 className="text-base font-semibold dark:text-white">Recent meetings <span className="ml-1 text-sm font-normal text-slate-400">{shown.length}</span></h2></div>{loading?<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{[1,2,3].map(x=><div key={x} className="h-48 animate-pulse rounded-xl bg-slate-200"/>)}</div>:shown.length?<motion.div layout className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{shown.map(m=><MeetingCard key={m.id} meeting={m}/>)}</motion.div>:<div className="rounded-xl border border-dashed border-slate-300 bg-white py-20 text-center dark:bg-slate-900"><p className="font-semibold dark:text-white">No meetings found</p><p className="mt-1 text-sm text-slate-500">Try a name such as “Product” or “Marketing”.</p></div>}</section></Shell>}
