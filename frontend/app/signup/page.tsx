'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { Eye, EyeOff } from 'lucide-react'; import { createWorkspaceUser } from '@/lib/supabase-auth';
export default function Signup(){const router=useRouter(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[show,setShow]=useState(false),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setNotice('');try{await createWorkspaceUser(name,email,password);router.push('/')}catch(err){setNotice(err instanceof Error?err.message:'Could not create profile.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-violet-50 p-5"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-card"><Link href="/" className="text-xl font-bold">Butterflies <span className="text-lilac">ai</span></Link><h1 className="mt-8 text-2xl font-bold">Create your profile</h1><p className="mt-2 text-sm text-slate-500">Save your name, email and password for this demo workspace.</p><form onSubmit={submit}><input value={name} onChange={e=>setName(e.target.value)} required className="mt-6 w-full rounded-lg border p-3" placeholder="Your name"/><input value={email} onChange={e=>setEmail(e.target.value)} required className="mt-3 w-full rounded-lg border p-3" placeholder="Email address" type="email"/><div className="relative mt-3"><input value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required className="w-full rounded-lg border p-3 pr-11" placeholder="Password (6+ characters)" type={show?'text':'password'}/><button type="button" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'} className="absolute right-3 top-3 text-slate-500">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>{notice&&<p className="mt-3 text-sm text-red-600">{notice}</p>}<button disabled={busy} className="mt-5 w-full rounded-lg bg-lilac p-3 text-sm font-semibold text-white disabled:opacity-60">{busy?'Saving…':'Create profile'}</button></form><p className="mt-5 text-center text-sm text-slate-500">Already registered? <Link className="font-semibold text-lilac" href="/login">Sign in</Link></p></section></main>}
