'use client';
import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { signUp } from '@/lib/supabase-auth';
export default function Signup(){const router=useRouter(),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[notice,setNotice]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setNotice('');try{await signUp(name,email,password);setNotice('Account created. Check your email to confirm your account, then sign in.');setTimeout(()=>router.push('/login'),1800)}catch(err){setNotice(err instanceof Error?err.message:'Could not create account.')}finally{setBusy(false)}};return <main className="grid min-h-screen place-items-center bg-violet-50 p-5"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-card"><Link href="/" className="text-xl font-bold">Butterflies <span className="text-lilac">ai</span></Link><h1 className="mt-8 text-2xl font-bold">Create your workspace</h1><p className="mt-2 text-sm text-slate-500">Start turning meetings into momentum.</p><form onSubmit={submit}><input value={name} onChange={e=>setName(e.target.value)} required className="mt-6 w-full rounded-lg border p-3" placeholder="Your name"/><input value={email} onChange={e=>setEmail(e.target.value)} required className="mt-3 w-full rounded-lg border p-3" placeholder="Email address" type="email"/><input value={password} onChange={e=>setPassword(e.target.value)} minLength={6} required className="mt-3 w-full rounded-lg border p-3" placeholder="Password (6+ characters)" type="password"/>{notice&&<p className="mt-3 text-sm text-slate-600">{notice}</p>}<button disabled={busy} className="mt-5 w-full rounded-lg bg-lilac p-3 text-sm font-semibold text-white disabled:opacity-60">{busy?'Creating account…':'Create account'}</button></form><p className="mt-5 text-center text-sm text-slate-500">Already have an account? <Link className="font-semibold text-lilac" href="/login">Sign in</Link></p></section></main>}
