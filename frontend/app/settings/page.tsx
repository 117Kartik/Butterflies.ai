'use client';
import { useEffect, useState } from 'react'; import Shell from '@/components/shell'; import { Moon, Sun } from 'lucide-react';
export default function Settings(){const [dark,setDark]=useState(false);useEffect(()=>{const enabled=localStorage.theme==='dark';setDark(enabled);document.documentElement.classList.toggle('dark',enabled)},[]);const toggle=()=>{const next=!dark;setDark(next);localStorage.theme=next?'dark':'light';document.documentElement.classList.toggle('dark',next)};return <Shell><section className="mx-auto max-w-3xl px-8 py-12"><p className="text-sm font-medium text-lilac">Workspace preferences</p><h1 className="mt-1 text-3xl font-bold dark:text-white">Settings</h1><p className="mt-2 text-slate-500">Personalize how Butterflies.ai looks and feels.</p><div className="mt-8 rounded-xl border border-slate-200 bg-white p-5 shadow-card dark:border-slate-800 dark:bg-slate-900"><div className="flex items-center justify-between"><div><h2 className="font-semibold dark:text-white">Appearance</h2><p className="mt-1 text-sm text-slate-500">Select the theme you prefer. Your choice stays active during this session.</p></div><button onClick={toggle} className="flex items-center gap-2 rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold dark:bg-slate-800 dark:text-white">{dark?<Sun size={16}/>:<Moon size={16}/>} {dark?'Use light theme':'Use dark theme'}</button></div></div></section></Shell>}
